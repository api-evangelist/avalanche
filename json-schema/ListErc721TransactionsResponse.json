{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/api-evangelist/avalanche/main/json-schema/ListErc721TransactionsResponse.json",
  "title": "ListErc721TransactionsResponse",
  "type": "object",
  "properties": {
    "nextPageToken": {
      "type": "string",
      "description": "A token, which can be sent as `pageToken` to retrieve the next page. If this field is omitted or empty, there are no subsequent pages."
    },
    "transactions": {
      "type": "array",
      "items": {
        "$ref": "#/components/schemas/Erc721Transfer"
      }
    }
  },
  "required": [
    "transactions"
  ]
}